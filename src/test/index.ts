/**
 * Test Utilities Index
 *
 * Import everything you need from here:
 *   import { renderWithProviders, createDocument, createPages } from '@/test'
 */

// Re-export factories
export * from './factories'

// Re-export render utilities
export * from './render'

// Re-export mocks from setup
export { mockElectronAPI } from './setup'
